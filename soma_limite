'''
programa que testa se numero maior que limite
'''

def soma_limite(a, b):
    return limite > a + b

x = int(input("x: "))
y = int(input("y: "))
limite = int(input("limite: "))

if (soma_limite(x, y)) == True:
    print("Soma menor que limite")
else:
    print("Soma maior ou igual que limite")
